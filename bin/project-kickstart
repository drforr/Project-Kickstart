#!/usr/bin/perl

use strict;
use warnings;
use Project::Kickstart;

our $VERSION = 0.01;

my $pk = Project::Kickstart->new;
$pk->init( \@ARGV );

my $mode = shift @ARGV;

$pk->run_plugin( $mode, \@ARGV );
